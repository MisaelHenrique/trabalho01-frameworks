import { Image } from "../Img";

export const CardProducts = ({product}) => {
    

    return(
        <div>
            <div key={product.id} className="product">

            </div>
        </div>
    );

}